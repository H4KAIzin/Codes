<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Codes.EditarCliente">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackLayout Grid.Row="0" Orientation="Horizontal" HorizontalOptions="Center">
            <Frame Padding="10" CornerRadius="10">
                </Frame>
            <Frame Padding="10" CornerRadius="10">
                </Frame>
        </StackLayout>

        <Button Text="Editar perfil" Grid.Row="1" HorizontalOptions="Center" />
        <Button Text="Excluir usuário?" Grid.Row="2" HorizontalOptions="Center" />
    </Grid>
</ContentPage>